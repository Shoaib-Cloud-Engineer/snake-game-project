apiVersion: v1
kind: Service
metadata:
  name: backend-svc
spec:
  selector:
    app: php-backend
  ports:
    - port: 80
      targetPort: 80   # must match containerPort
  type: NodePort       # or LoadBalancer depending on how you want to expose
